# Snapvest 開發進度記錄

## 最新更新（2024）

### ✅ 已完成的工作

#### 1. 配色系統更新（14色色卡）
- ✅ 更新 `ColorTheme.swift` 使用用戶提供的14個正確色號
  - 左側深藍區：`#0D2235`, `#132A42`, `#1B3C59`, `#1D4C6A`
  - 中段藍綠區：`#1F6A8A`, `#2A87A6`, `#36A2C6`, `#5FBBD5`
  - 右側深綠區：`#8AC0B3`, `#4CA19E`, `#358077`, `#1F6E5F`, `#1B4D3E`
- ✅ 更新圓餅圖顏色數組為14色
- ✅ 更新所有顏色選擇器為14色配色系統

#### 2. 首頁卡片顏色更新
- ✅ 淨資產卡片：使用 `blue3` (#1B3C59) 作為主要顏色
- ✅ 投資資產卡片：使用 `green4` (#1F6E5F) 作為主要顏色
- ✅ 現金卡片：使用 `blueGreen4` (#5FBBD5) 和 `blueGreen3` (#36A2C6) 作為TWD/USD顏色
- ✅ 所有卡片背景改為 `Color.cardBackground`（極淺藍，50%透明度）

#### 3. 帳戶分頁顏色更新
- ✅ 美金帳戶：使用 `blueGreen2` (#2A87A6) 作為顏色
- ✅ 加密貨幣錢包：使用 `green3` (#358077) 作為顏色（較暗）
- ✅ 所有帳戶卡片背景改為 `Color.cardBackground`
- ✅ 更新帳戶卡片文字顏色使用對應的深色

#### 4. 資產分頁顏色更新
- ✅ 台股類別：使用 `blue3` (#1B3C59) - 深藍色
- ✅ 美股類別：使用 `blueGreen2` (#2A87A6) - 藍綠色
- ✅ 加密貨幣類別：使用 `green3` (#358077) - 深青綠色（較暗）
- ✅ 所有資產卡片背景改為 `Color.cardBackground`
- ✅ 更新資產卡片文字顏色使用對應的深色

#### 5. 圓餅圖顏色功能
- ✅ 預設使用14色配色系統，按市值從大到小輪流分配
- ✅ 恢復自定義顏色選擇功能（用戶可以選擇顏色並應用到圓餅圖）
- ✅ 顏色選擇器更新為14色選項
- ✅ 修復 `colorUpdateTrigger` 未定義的錯誤

#### 6. 漲跌幅顏色
- ✅ 漲幅綠色：`Color(red: 0.0, green: 0.7, blue: 0.3)` - 正常綠色
- ✅ 跌幅紅色：`Color(red: 1.0, green: 0.2, blue: 0.2)` - 亮紅色

#### 7. 背景顏色系統
- ✅ 主背景：`bluePale.opacity(0.3)` - 極淺藍，30%透明度
- ✅ 卡片背景：`bluePale.opacity(0.5)` - 極淺藍，50%透明度
- ✅ 次要背景：`bluePale.opacity(0.4)` - 極淺藍，40%透明度
- ✅ 所有卡片不再使用純白背景

---

## 🔄 待完成的工作

### 1. 顏色系統檢查
- ⏳ 確認所有按鈕顏色都使用14色（目前 `appPrimary` 使用 `blue4`）
- ⏳ 確認所有圖標顏色都使用14色
- ⏳ 檢查是否有遺漏的硬編碼顏色（如 `Color.orange`, `Color.purple` 等）

### 2. 顏色一致性檢查
- ⏳ 確認所有視圖中的顏色都符合14色配色系統
- ⏳ 檢查 `AccountDetailView` 中的顏色是否已更新
- ⏳ 檢查 `HoldingDetailView` 中的顏色是否已更新
- ⏳ 檢查其他視圖（如 `TransactionsView`, `TransferView` 等）中的顏色

### 3. 功能完善
- ⏳ 實現即時匯率獲取（目前使用固定模擬值 32）
- ⏳ 實現價格服務的即時價格獲取
- ⏳ 實現買入/賣出功能（目前只有按鈕，功能未實現）
- ⏳ 完善 FIFO 批次快照更新邏輯

### 4. UI/UX 優化
- ⏳ 檢查所有動畫是否流暢
- ⏳ 檢查所有卡片樣式是否一致
- ⏳ 檢查文字大小和顏色層次是否清晰

---

## 📝 技術債務

1. **匯率服務**：目前使用固定模擬值，需要實現即時匯率獲取
2. **價格服務**：需要實現即時價格獲取
3. **快照更新時機**：需要確認所有快照更新邏輯是否正確
4. **顏色硬編碼**：可能還有一些地方使用硬編碼顏色，需要全面檢查

---

## 🎨 顏色使用規範

### 14色配色系統
- **左側深藍區**：`blue1` ~ `blue4` - 用於台股、主要文字等
- **中段藍綠區**：`blueGreen1` ~ `blueGreen4` - 用於美股、現金等
- **右側深綠區**：`green1` ~ `green5` - 用於加密貨幣、投資資產等

### 例外顏色（允許使用）
- **漲幅綠色**：`Color(red: 0.0, green: 0.7, blue: 0.3)` - 正常綠色
- **跌幅紅色**：`Color(red: 1.0, green: 0.2, blue: 0.2)` - 亮紅色

### 背景顏色
- **主背景**：`bluePale.opacity(0.3)`
- **卡片背景**：`bluePale.opacity(0.5)`
- **次要背景**：`bluePale.opacity(0.4)`

---

## 📌 注意事項

1. 所有 UI 元素（按鈕、圖標、背景、文字等）必須使用14色配色系統中的顏色
2. 只有漲跌幅可以使用例外顏色（綠色和紅色）
3. 圓餅圖預設使用14色，但用戶可以自定義選擇
4. 所有卡片背景不再使用純白，統一使用 `Color.cardBackground`
